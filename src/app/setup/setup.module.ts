import { NgModule } from '@angular/core';

import { SetupRoutingModule } from './setup-routing.module';
import { SetupService } from './setup.service';

@NgModule({
  declarations: [],
  imports: [SetupRoutingModule],
  providers: [SetupService],
})
export class SetupModule {}
