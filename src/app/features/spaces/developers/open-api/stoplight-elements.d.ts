/**
 * Stoplight Elements ships a prebuilt web-component bundle without type declarations.
 *
 * It is imported for its side effect only - loading it registers the `<elements-api>` custom
 * element - so an empty module declaration is all the compiler needs.
 */
declare module '@stoplight/elements/web-components.min.js';
