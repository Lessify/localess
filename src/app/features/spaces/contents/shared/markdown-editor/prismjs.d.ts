/**
 * PrismJS ships no type declarations and is imported purely for its side effect - loading it
 * assigns the global `Prism` that ngx-markdown looks for when highlighting code blocks.
 */
declare module 'prismjs';
